# Dynamic Programming - 2D Patterns

> **Trigger**: Grid problems, two sequences, substrings/subsequences comparison

---

## Template: Grid DP

```cpp
int gridDP(vector<vector<int>>& grid) {
    int m = grid.size(), n = grid[0].size();
    vector<vector<int>> dp(m, vector<int>(n, 0));
    
    // Base: first row and column
    dp[0][0] = grid[0][0];
    for (int i = 1; i < m; i++) dp[i][0] = dp[i-1][0] + grid[i][0];
    for (int j = 1; j < n; j++) dp[0][j] = dp[0][j-1] + grid[0][j];
    
    // Fill rest
    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            dp[i][j] = grid[i][j] + /* combine from dp[i-1][j], dp[i][j-1] */;
        }
    }
    return dp[m-1][n-1];
}
```

---

## Example 1: Unique Paths

**LeetCode 62** | O(m×n) time

```cpp
int uniquePaths(int m, int n) {
    vector<vector<int>> dp(m, vector<int>(n, 1));
    
    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            dp[i][j] = dp[i-1][j] + dp[i][j-1];
        }
    }
    return dp[m-1][n-1];
}
```

---

## Example 2: Minimum Path Sum

**LeetCode 64** | O(m×n) time

```cpp
int minPathSum(vector<vector<int>>& grid) {
    int m = grid.size(), n = grid[0].size();
    
    for (int i = 1; i < m; i++) grid[i][0] += grid[i-1][0];
    for (int j = 1; j < n; j++) grid[0][j] += grid[0][j-1];
    
    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            grid[i][j] += min(grid[i-1][j], grid[i][j-1]);
        }
    }
    return grid[m-1][n-1];
}
```

---

## Example 3: Longest Common Subsequence

**LeetCode 1143** | O(m×n) time

```cpp
int longestCommonSubsequence(string text1, string text2) {
    int m = text1.size(), n = text2.size();
    vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));
    
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (text1[i-1] == text2[j-1]) {
                dp[i][j] = dp[i-1][j-1] + 1;
            } else {
                dp[i][j] = max(dp[i-1][j], dp[i][j-1]);
            }
        }
    }
    return dp[m][n];
}
```

---

## Example 4: Edit Distance

**LeetCode 72** | O(m×n) time

```cpp
int minDistance(string word1, string word2) {
    int m = word1.size(), n = word2.size();
    vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));
    
    // Base: empty string transformations
    for (int i = 0; i <= m; i++) dp[i][0] = i;
    for (int j = 0; j <= n; j++) dp[0][j] = j;
    
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (word1[i-1] == word2[j-1]) {
                dp[i][j] = dp[i-1][j-1];
            } else {
                dp[i][j] = 1 + min({
                    dp[i-1][j],   // delete
                    dp[i][j-1],   // insert
                    dp[i-1][j-1]  // replace
                });
            }
        }
    }
    return dp[m][n];
}
```

---

## 2D DP Patterns Summary

| Problem Type | Recurrence Pattern |
|--------------|-------------------|
| Grid paths | dp[i][j] = dp[i-1][j] + dp[i][j-1] |
| LCS | Match: dp[i-1][j-1]+1, Else: max(left, up) |
| Edit Distance | Match: dp[i-1][j-1], Else: 1+min(three dirs) |
| Palindrome | dp[i][j] = dp[i+1][j-1] if match |

---

## Practice Problems

| Problem | Difficulty | Key Insight |
|---------|------------|-------------|
| LC 62 - Unique Paths | Medium | Count paths |
| LC 64 - Minimum Path Sum | Medium | min(up, left) + current |
| LC 1143 - LCS | Medium | Two sequence comparison |
| LC 72 - Edit Distance | Medium | 3 operations |
| LC 516 - Longest Palindromic Subsequence | Medium | LCS with reversed string |
